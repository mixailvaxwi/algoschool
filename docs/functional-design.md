# AlgoSchool — проектирование функциональности

Документ описывает целевую функциональность платформы: акторов, доменные объекты,
полный перечень сценариев использования, изменения в модели данных и порядок
реализации.

Отправная точка — то, что уже работает: курсы из модулей, уроков и шагов; шаги
четырёх типов (теория, код, текстовый ответ, выбор варианта); запись на курс
самостоятельно или по заявке; отправка решений с проверкой кода в Ejudge;
бинарный прогресс по шагу. Всё остальное ниже — надстройка над этим, а не замена.

---

## 1. Акторы

| Актор | Кто это | Как получает роль |
|---|---|---|
| **Гость** | Неавторизованный посетитель | — |
| **Студент** | Учится и решает задачи | Роль по умолчанию при регистрации |
| **Преподаватель (автор)** | Владелец курса: содержание, состав, оценки | Назначает администратор |
| **Соавтор** | Правит содержание чужого курса | Приглашает автор курса |
| **Ассистент (TA)** | Проверяет решения, отвечает на вопросы, не правит курс | Назначает автор курса |
| **Администратор** | Пользователи, роли, модерация, системные настройки | Выдаётся в базе / другим администратором |
| **Система** | Фоновые процессы: опрос вердиктов, дедлайны, рассылки, агрегаты | — |

**Ключевое изменение в модели прав.** Сейчас роль глобальна (`users.role`), и
`/api/teacher/**` открыт любому `ROLE_TEACHER`, а принадлежность курса
проверяется отдельно в `CourseAccessService`. Соавтор и ассистент — это роли
*внутри курса*, глобальной ролью они не выражаются. Нужна таблица членства:

```
course_members(course_id, user_id, role)   role ∈ {OWNER, COAUTHOR, ASSISTANT, STUDENT}
```

Глобальная роль остаётся как грубый фильтр маршрутов, точные права — из членства.
`CourseAccessService` остаётся единственным местом проверки — расширяется, не
дублируется.

---

## 2. Доменные объекты

### 2.1 Что есть сейчас

```
Course ──< Module ──< Lesson ──< Step
                                 ├── TheoryStep      (content)
                                 └── Problem         (description, счётчики)
                                     ├── CodeProblem   (лимиты, ejudge ids)
                                     ├── TextProblem   (correctAnswer)
                                     └── ChoiceProblem (options, correctOptionIndex)

UserCourse (запись), CourseApplication (заявка),
Submission (попытка + вердикт), UserStepProgress (бинарный прогресс)
```

### 2.2 Банк задач **[сделано, этап 1]**

Задача больше не наследуется от `Step`. Раньше строка задачи физически
принадлежала одному уроку: поставить одну задачу в два урока можно было только
копированием, копия расходилась с оригиналом при правке условия, а статистика
(`attempted_students_count`, `success_students_count`) размазывалась по копиям —
сложность задачи переставала быть измеримой.

```
Problem  — единица содержания и оценивания, живёт в банке задач
├── id, authorId, title, description, tags[], difficulty, maxScore, visibility
└── подтипы (JOINED):
    CodeProblem, ChoiceProblem, TextProblem
    (NumericProblem, MatchingProblem, OrderingProblem, OpenAnswerProblem — этап 3)

Step — размещение содержания в уроке
├── TheoryStep   (content)
└── ProblemStep  (problem_id)   ← вместо Problem extends Step
```

Что из этого следует по смыслу:

- **Правка задачи меняет её во всех уроках сразу.** Это и есть цель банка;
  редактор показывает, сколько уроков затронет правка.
- **Удаление шага снимает задачу с урока, но не из банка.** Прогресс по снятому
  размещению удаляется, отправленные решения остаются: они привязаны к задаче.
- **Прогресс живёт на размещении, история попыток и счётчики — на задаче.**
  Решив задачу в одном уроке, студент видит её пройденной во всех уроках,
  куда он записан; в счётчике решивших он при этом учитывается один раз.
- **Видимость:** `PRIVATE` или `PUBLIC`. Промежуточного «видна участникам
  курса» нет — для него нужна таблица членства (этап 4).

Остальные новые сущности:

| Сущность | Назначение |
|---|---|
| `Group` / `GroupMember` | Поток/группа внутри курса |
| `Deadline` | Срок на урок или задачу, опционально для группы |
| `Grade` / `GradeItem` | Журнал: оценка за элемент, формула итоговой |
| `Notification` | Уведомление в интерфейсе + канал доставки |
| `AuditEvent` | Кто что изменил (роли, оценки, ререйдж) |

`Submission` расширяется баллами и полями ручной проверки — сейчас там только
вердикт:

```
Submission += score, max_score                  — баллы, а не только «решено/нет»
           += reviewed_by, review_comment       — для развёрнутых ответов
```

---

## 3. Сценарии использования

Идентификаторы: `UC-<актор>-<номер>`. Помечено: **[есть]** — реализовано,
**[частично]** — работает не полностью, без пометки — новое.

### 3.1 Гость

| ID | Сценарий | Правило |
|---|---|---|
| UC-G-01 | Просмотр каталога курсов **[есть]** | Видны только опубликованные |
| UC-G-02 | Просмотр карточки курса и программы **[есть]** | Черновик отдаёт 404, а не 403 |
| UC-G-03 | Поиск и фильтры каталога (тема, сложность, язык, автор, открытость) | |
| UC-G-04 | Регистрация **[есть]** | Роль от клиента не принимается |
| UC-G-05 | Подтверждение e-mail по ссылке | До подтверждения — доступ только к каталогу |
| UC-G-06 | Вход **[есть]** | |
| UC-G-07 | Восстановление пароля по e-mail | Токен одноразовый, TTL 1 час |
| UC-G-08 | Попытка открыть урок или отправить решение **[есть]** | 401 в едином формате ошибки |
| UC-G-09 | Просмотр публичного профиля **[частично]** | Сейчас требует входа; открыть по флагу приватности |

### 3.2 Студент — аккаунт

| ID | Сценарий | Правило |
|---|---|---|
| UC-S-01 | Просмотр и правка профиля **[есть]** | |
| UC-S-02 | Смена пароля | Требует текущий пароль, инвалидирует прочие сессии |
| UC-S-03 | Смена e-mail | Подтверждение на новый адрес |
| UC-S-04 | Загрузка аватара | Лимит размера, проверка типа |
| UC-S-05 | Настройка уведомлений | По каналам и по типам событий |
| UC-S-06 | Выход, просмотр активных сессий, отзыв сессии | Требует refresh-токенов (см. §5) |
| UC-S-07 | Удаление аккаунта | Решения обезличиваются, не удаляются: иначе рушится статистика задач |

### 3.3 Студент — курсы

| ID | Сценарий | Правило |
|---|---|---|
| UC-S-10 | Запись на открытый курс **[есть]** | Повторная запись идемпотентна |
| UC-S-11 | Заявка на закрытый курс с сопроводительным письмом **[есть]** | Одна активная заявка на курс |
| UC-S-12 | Отзыв своей заявки | Только в статусе `PENDING` |
| UC-S-13 | Список моих заявок **[есть]** | |
| UC-S-14 | Список моих курсов **[есть]** | |
| UC-S-15 | Вход по коду приглашения | Код одноразовый или с лимитом использований и сроком |
| UC-S-16 | Отписка от курса | Прогресс сохраняется на случай возврата |
| UC-S-17 | Продолжить с последнего незавершённого шага | Курсор хранится на сервере, а не в localStorage |

### 3.4 Студент — обучение

| ID | Сценарий | Правило |
|---|---|---|
| UC-S-20 | Прохождение урока в плеере **[есть]** | |
| UC-S-21 | Отметка теории прочитанной **[есть]** | |
| UC-S-22 | Отправка решения задачи **[есть]** | Только при записи на курс |
| UC-S-23 | Получение вердикта по коду **[есть]** | Асинхронно: `PENDING` → вердикт из Ejudge |
| UC-S-24 | Просмотр истории попыток **[есть]** | Вывод компилятора и результаты тестов |
| UC-S-25 | Повторная отправка после неудачи **[есть]** | Ограничение частоты: N попыток в минуту |
| UC-S-26 | Множественный выбор ответа **[частично]** | `isMultipleChoice` хранится, но не проверяется и не рисуется |
| UC-S-27 | Ответ на задачи новых типов: число с допуском, соответствие, упорядочивание, развёрнутый ответ | Развёрнутый проверяется вручную (UC-T-40) |
| UC-S-28 | Просмотр прогресса по курсу **[частично]** | Есть по шагу; нужен агрегат по курсу и модулю |
| UC-S-29 | Просмотр дедлайнов и оставшегося времени | Источник времени — сервер |
| UC-S-30 | Вопрос к шагу в обсуждении, ответ преподавателя или другого студента | Модерация автором курса |
| UC-S-31 | Заметки и закладки на шаге | Приватны |
| UC-S-32 | Жалоба на задачу (опечатка, неверные тесты) | Попадает в очередь автору |
| UC-S-33 | Просмотр своих оценок по курсу **[есть]** | Журнал в режиме чтения, с комментарием к ручной оценке |
| UC-S-34 | Получение сертификата о прохождении курса | По достижении критерия завершения (§4.3) |

### 3.5 Преподаватель — курс и содержание

| ID | Сценарий | Правило |
|---|---|---|
| UC-T-01 | Создание, правка, удаление курса **[есть]** | |
| UC-T-02 | Публикация и снятие с публикации **[есть]** | Снятие не отписывает записанных |
| UC-T-03 | CRUD модулей, уроков, шагов **[есть]** | |
| UC-T-04 | Изменение порядка перетаскиванием | Пакетное обновление `orderIndex` одной транзакцией |
| UC-T-05 | Предпросмотр курса глазами студента | Без записи на курс и без порчи статистики |
| UC-T-06 | Копирование курса или модуля | Задачи переиспользуются из банка, а не дублируются |
| UC-T-07 | Импорт/экспорт курса (JSON/ZIP) | Для переноса между инсталляциями |
| UC-T-08 | Приглашение соавтора и ассистента | Права по таблице членства |
| UC-T-09 | Настройка критерия завершения курса и формулы оценки | См. §4.3 |
| UC-T-10 | Объявление всем студентам курса | Уведомление + лента курса |

### 3.6 Преподаватель — банк задач

| ID | Сценарий | Правило |
|---|---|---|
| UC-T-20 | Создание задачи любого типа **[есть]** | Ответ и тесты никогда не уходят клиенту (`WRITE_ONLY`) |
| UC-T-21 | Теги, сложность, видимость **[есть]** | Видимость личная / публичная; «видна курсу» — вместе с членством (этап 4) |
| UC-T-22 | Поиск по банку и вставка задачи в урок **[есть]** | Одна задача — много размещений; чужую можно вставить, если она публичная |
| UC-T-23 | Привязка задачи к Ejudge (`contest_id`, `problem_id`) **[есть]** | Проверка доступности при сохранении |
| UC-T-24 | Пробное решение своей задачи | Отправка от имени автора, не влияет на счётчики |
| UC-T-25 | Версионирование задачи | Правка условия не меняет уже выставленные баллы |
| UC-T-26 | Просмотр статистики задачи **[частично]** | Есть счётчики попыток и успехов; нужен разрез по курсам и распределение вердиктов |
| UC-T-27 | Ререйдж всех решений задачи | После смены тестов; пересчёт прогресса и оценок |

### 3.7 Преподаватель — проверка и оценивание

| ID | Сценарий | Правило |
|---|---|---|
| UC-T-40 | Ручная проверка развёрнутых ответов | Очередь проверки, критерии, комментарий |
| UC-T-41 | Ручная корректировка балла с обязательным комментарием **[частично]** | Комментарий обязателен и виден студенту; аудит-лог — этап 5 |
| UC-T-42 | Просмотр решений студентов **[частично]** | Есть общий список; нужны фильтры по курсу, задаче, студенту, вердикту |
| UC-T-43 | Журнал оценок по курсу **[есть]** | Строки — студенты, столбцы — оцениваемые элементы |
| UC-T-44 | Формула итоговой оценки и веса групп элементов **[частично]** | Вес задаётся поэлементно; группы элементов — см. §4.3 |
| UC-T-45 | Экспорт журнала **[есть]** | CSV; XLSX не реализован |
| UC-T-46 | Проверка на заимствования среди решений задачи | Сравнение внутри выборки, отчёт с парами |
| UC-T-47 | Индивидуальное продление дедлайна | Для оговорённых случаев |

### 3.8 Преподаватель — люди и группы

| ID | Сценарий | Правило |
|---|---|---|
| UC-T-60 | Список заявок и решение по ним **[есть]** | Добавить комментарий и массовые действия |
| UC-T-61 | Список студентов курса с прогрессом | |
| UC-T-62 | Создание групп и распределение студентов | |
| UC-T-63 | Дедлайны на группу | |
| UC-T-64 | Отчисление студента с курса | |
| UC-T-65 | Аналитика: воронка по шагам, сложные задачи, активность, время на шаг | |

### 3.9 Ассистент (TA)

Подмножество прав преподавателя без изменения содержания:
UC-T-40, UC-T-41 (проверка и корректировка), UC-T-42, UC-T-60, UC-T-61,
UC-S-30 (ответы в обсуждениях).
Не может: править курс и задачи, публиковать, менять формулу оценки, удалять данные.

### 3.10 Администратор

| ID | Сценарий | Правило |
|---|---|---|
| UC-A-01 | Поиск пользователей, просмотр карточки | Сейчас страница-заглушка во фронтенде |
| UC-A-02 | Выдача и снятие ролей | Заменяет ручной `UPDATE users SET role=...` из README |
| UC-A-03 | Блокировка и разблокировка аккаунта | `isAccountNonLocked` в `User` уже предусмотрен, но всегда `true` |
| UC-A-04 | Принудительный сброс пароля | |
| UC-A-05 | Модерация курсов: снятие с публикации | С уведомлением автора |
| UC-A-06 | Настройки Ejudge: адрес, ключ, соответствие языков | Сейчас только через `application.yml` |
| UC-A-07 | Состояние очереди проверки, зависшие `PENDING`, ручной перезапрос | |
| UC-A-08 | Просмотр аудит-лога | Роли, оценки, ререйдж |
| UC-A-09 | Системные метрики и здоровье | Actuator |
| UC-A-10 | Рассылка объявления всем пользователям | |

### 3.11 Система (фоновые процессы)

| ID | Процесс | Правило |
|---|---|---|
| UC-SYS-01 | Опрос вердиктов Ejudge **[есть]** | Не защищён от параллельного запуска — нужен ShedLock |
| UC-SYS-02 | Повторная отправка решений со статусом `SUBMISSION_FAILED` | С возрастающей паузой и потолком попыток |
| UC-SYS-03 | Напоминания о дедлайнах | За сутки и за час |
| UC-SYS-04 | Доставка уведомлений | Внутренние + e-mail |
| UC-SYS-05 | Ночная агрегация статистики по задачам и курсам | Разгружает интерактивные запросы |
| UC-SYS-06 | Прогон антиплагиата по выборке решений | |
| UC-SYS-07 | Чистка просроченных токенов и черновиков | |

---

## 4. Оценивание и прогресс

### 4.1 Что было не так **[закрыто, этап 2]**

`UserStepProgress.isCompleted` — булев флаг, `Submission` не хранил баллов.
Пока задача либо решена, либо нет, невозможны частичные баллы, вес задачи в
курсе и журнал оценок.

### 4.2 Модель баллов **[сделано, этап 2]**

```
Submission.score / max_score      — за конкретную отправку
Grade(user, grade_item)           — зачтённый балл за элемент по политике
GradeItem                         — задача урока | ручной столбец журнала,
                                    max_score = вес элемента в этом курсе
```

Политика зачёта задаётся на уровне элемента: лучшая попытка, последняя, первая,
средняя. Что из этого следует по смыслу:

- **Балл и вердикт — разные вещи.** `score = NULL` означает «балла ещё нет»
  (`PENDING`, либо `SUBMISSION_FAILED` — сбой Ejudge, а не вердикт по ответу);
  ноль означает «проверено, не засчитано». В политику зачёта NULL не попадает,
  поэтому недоступность проверяющей системы не портит оценку.
- **Вес элемента — свойство курса, а не задачи.** Одна и та же задача из банка
  может стоить в разных курсах по-разному; политика считает в долях от
  максимума попытки, поэтому смена веса пересчитывает баллы, а не ломает их.
- **Зачёт следует за задачей, а не за размещением** — как и отметка о
  прохождении (см. §2.2). Решив задачу в одном курсе, студент приходит с этим
  баллом и во второй, где она стоит; при зачислении оценки пересчитываются.
- **Ручная оценка замораживает клетку** (UC-T-41): автоматический пересчёт её
  не трогает, пока преподаватель не снимет оценку. Комментарий обязателен и
  виден студенту. Аудит-лог такой правки — этап 5.
- Пока задача либо решена, либо нет, балл равен весу или нулю. Хранится он уже
  у отправки, поэтому частичный балл — это вопрос типов задач (этап 3), а не
  модели.

### 4.3 Критерий завершения курса

Настраивается автором, варианты:

- доля пройденных шагов ≥ X %;
- все обязательные шаги пройдены;
- итоговый балл по формуле ≥ порога;
- комбинация условий.

Итоговая оценка = взвешенная сумма по группам элементов (теория, задачи) с
настраиваемыми весами и возможностью отбросить N худших результатов.

Сейчас итог считается проще: сумма зачтённых баллов к сумме весов элементов
курса. Групп элементов и отбрасывания худших результатов ещё нет — они требуют
настройки курса (UC-T-09) и появятся вместе с ней.

---

## 5. Изменения в API

Существующие маршруты не ломаются; добавляются новые разделы.

```
# Банк задач                                     [есть, кроме ререйджа]
GET    /api/teacher/problems?q=&type=&difficulty=&tag=&onlyMine=
GET    /api/teacher/problems/tags
GET    /api/teacher/problems/{id}
POST   /api/teacher/problems
PUT    /api/teacher/problems/{id}
DELETE /api/teacher/problems/{id}               # только пока не в уроках и без решений
POST   /api/teacher/problems/{id}/rejudge

# Размещение задачи в уроке                      [есть]
# тот же маршрут шагов, но с problemId вместо содержания задачи
POST   /api/teacher/courses/{id}/lessons/{id}/steps  {"stepType":..., "problemId":...}

# Проверка и оценки                              [журнал есть; очередь ручной
#                                                 проверки — этап 3]
GET    /api/teacher/submissions?courseId=&problemId=&userId=&status=
GET    /api/teacher/review-queue                # развёрнутые ответы на проверку
GET    /api/teacher/courses/{id}/gradebook      [есть]
GET    /api/teacher/courses/{id}/gradebook/export   [есть] CSV
GET    /api/teacher/courses/{id}/grade-items    [есть]
POST   /api/teacher/courses/{id}/grade-items    [есть] ручной столбец журнала
PUT    /api/teacher/grade-items/{id}            [есть] название, вес, политика
DELETE /api/teacher/grade-items/{id}            [есть] только ручной столбец
PUT    /api/teacher/grades                      [есть] ручной балл + причина
DELETE /api/teacher/grades/{itemId}/{userId}    [есть] снять ручной балл
GET    /api/courses/{id}/my-grades              [есть]

# Группы и дедлайны
POST   /api/teacher/courses/{id}/groups
PUT    /api/teacher/courses/{id}/deadlines
POST   /api/teacher/courses/{id}/members        # соавтор, ассистент

# Потоковые обновления
GET    /api/submissions/stream                  # SSE: готовые вердикты

# Администрирование
GET    /api/admin/users?q=
PUT    /api/admin/users/{id}/role
POST   /api/admin/users/{id}/block
GET    /api/admin/queue                         # зависшие PENDING
GET    /api/admin/audit
```

Правила, обязательные для новых маршрутов:

- **Время задаёт сервер.** Клиент не присылает время сдачи или наступления дедлайна.
- **Ограничение частоты** на отправку решений — по пользователю и по задаче.
- **Ответы и тесты не покидают сервер.**
- Единый формат ошибок сохраняется (`ErrorResponse`).
- Для доступа к сессиям и отзыву входов нужны refresh-токены: сейчас JWT
  самодостаточен и отозвать его нельзя.

---

## 6. Миграция модели

Разделение `Problem` и `Step` — самое дорогое изменение. Сделано одной
миграцией **V7**, без копирования строк:

1. `step_problems` переименована в `problems`, внешний ключ на `steps` снят,
   у таблицы появилась собственная последовательность `id`. Прежние
   идентификаторы сохранены, поэтому внешние ключи детей (`choice_problems`,
   `code_problems`, `text_problems`, `submissions`) переехали сами.
2. Добавлены `title`, `author_id`, `difficulty`, `visibility`, `max_score`,
   `created_at`, `updated_at` и таблица `problem_tags`. Автор проставлен от
   автора курса, название — из первой строки условия.
3. Создана `problem_steps(id → steps.id, problem_id → problems.id)`; для каждой
   существующей задачи заведено размещение `(id, id)` — её прежний шаг.
4. У `submissions` появился `step_id` (где решали) при сохранённом `problem_id`
   (что решали). Колонка допускает NULL и обнуляется при снятии задачи с урока:
   решение принадлежит задаче и переживает размещение.

**V8** добавила баллы: `submissions.score` / `max_score` (заполнены по вердикту),
`grade_items` (по столбцу на каждое размещение задачи, в порядке прохождения
курса) и `grades` (перенос накопленной истории по политике «лучшая попытка»,
только записанным на курс).

Дальше по этапам: `course_members`, `groups`, `deadlines` — этап 4.

Каждая миграция — как `V2`: сначала разрешение конфликтов данных, потом
ограничения. Hibernate остаётся в режиме `validate`.

---

## 7. Нефункциональные требования

| Тема | Требование |
|---|---|
| Время | Единственный источник — сервер; все дедлайны в UTC, отображение в часовом поясе пользователя |
| Параллелизм | Фоновые задачи под ShedLock: опрос вердиктов уже сейчас не защищён от второго экземпляра |
| Реальное время | SSE вместо поллинга для готовых вердиктов |
| Устойчивость | Недоступность Ejudge не теряет отправку: очередь и ретраи |
| Честность | Ограничение частоты, лимит попыток, антиплагиат |
| Приватность | Чужие решения недоступны студенту; развёрнутые ответы видит только проверяющий |
| Аудит | Смена ролей, оценок, ререйдж — с автором и причиной |
| Доступность | Плеер проходится с клавиатуры |
| Данные | Экспорт своих данных и обезличивание при удалении аккаунта |

---

## 8. Очерёдность реализации

| Этап | Содержание | Зачем сначала |
|---|---|---|
| **0** ✅ | Множественный выбор (UC-S-26), ShedLock (UC-SYS-01), админ-панель (UC-A-01…04) | Закрывает известные ограничения из README, не требует новой модели |
| **1** ✅ | Банк задач: разделение `Problem` / `Step`, миграция V7 | База для переиспользования задач; чем позже, тем дороже |
| **2** ✅ | Баллы в `Submission`, `GradeItem`, журнал оценок | Снимает ограничение «решено / не решено» |
| **3** | Новые типы задач и ручная проверка развёрнутых ответов | Опирается на банк задач и баллы |
| **4** | Группы, дедлайны, соавторы и ассистенты | |
| **5** | Обсуждения, уведомления, аналитика, антиплагиат, сертификаты | |
