# Адрес бэкенда. Скопируйте в .env и поправьте под своё окружение:
#   cp .env.example .env
# Без файла .env используется значение по умолчанию из src/api/axios.ts
VITE_API_URL=http://localhost:8080/api
