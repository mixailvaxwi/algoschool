package com.algoschool.problem.entity;

/**
 * Сложность, объявленная автором. Это ориентир для подбора задачи в урок, а не
 * измеренная величина: измеренная считается из счётчиков попыток и решений.
 */
public enum Difficulty {
    EASY,
    MEDIUM,
    HARD
}
