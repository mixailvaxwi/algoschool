package com.algoschool.grade.entity;

/** Откуда берётся балл за элемент журнала. */
public enum GradeItemKind {
    /** Задача, размещённая в уроке: балл считается из решений студента. */
    PROBLEM,
    /** Столбец, который преподаватель заполняет руками (экзамен, проект, работа на семинаре). */
    MANUAL
}
